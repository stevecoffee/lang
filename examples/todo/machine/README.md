# machine/ (emitted classic code)

This directory is the **output** of a closed-context compile run.

- **Do not** hand-author product requirements here; edit `../meta.md` instead.  
- **Inputs to compile:** `docs/language.md` + `../meta.md` + `skills/metacompile/SKILL.md` only.  
- After compile, expect source, tests, and `COMPILE_REPORT.md` here.

Empty until the first successful metacompile.

# sandbox

Kind: collection (file `src/sandbox.md` + directory `src/sandbox/` — does not compile to a code file)

## Purpose

google-keep-tools modules for **sandbox**.

## Children

- [clear_sandbox](sandbox/clear_sandbox.md) ← `clear_sandbox.py`
- [purge_sandbox](sandbox/purge_sandbox.md) ← `purge_sandbox.py`

# Agent

- Collection file is `src/sandbox.md` (sibling of directory `src/sandbox/`); children are only inside `sandbox/`.
- Grouping is by concern, not original source tree paths.
